[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
select = ["E","F","I","B","UP"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["bidpredict", "bidparse"]
